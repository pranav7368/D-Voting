{
  "name": "@dvoting/trustee",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A single election trustee: holds one threshold key share and contributes one proven partial decryption to the count.",
  "scripts": {
    "start": "node src/server.ts",
    "test": "node --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@dvoting/crypto": "*",
    "@dvoting/ledger": "*",
    "@hono/node-server": "^2.1.0",
    "hono": "^4.13.1",
    "zod": "^3.24.1"
  }
}
